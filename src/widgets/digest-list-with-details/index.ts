/**
 * Public API for the `widgets/digest-list-with-details` slice.
 *
 * This widget composes:
 * - Digest list (entity UI).
 * - Digest details drawer (feature UI).
 *
 * It owns the local presentation state (selected digest + drawer open/close).
 */
export { DigestListWithDetails } from './ui/DigestListWithDetails'
export type { DigestListWithDetailsProps } from './ui/DigestListWithDetails'
