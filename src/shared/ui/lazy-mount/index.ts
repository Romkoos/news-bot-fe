/**
 * Public API for the `shared/ui/lazy-mount` slice.
 *
 * This slice provides a small helper component for lazily mounting content
 * when it approaches the viewport.
 */
export { LazyMount } from './ui/LazyMount'
export type { LazyMountProps } from './ui/LazyMount'
