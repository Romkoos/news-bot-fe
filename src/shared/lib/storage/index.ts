/**
 * Public API for the `shared/lib/storage` segment.
 *
 * Typed localStorage access helpers.
 */
export {
  getStorage,
  getStoredJson,
  getStoredString,
  removeStoredValue,
  setStoredJson,
  setStoredString,
} from './storage'
